# Curriculum with FastAPI

## Settings

### Create virtualization

` python3 -m venv venv `

` source venv/bin/activate `

### Install dependencies
` pip install -r requirements.txt `

### Run server
` uvicorn main:app --reload `
